// Product
export interface Product {
    id: number;
    name: string;
    price: string;
    imageUrl: string;
    storeUrl: string;
}

export interface Project {
    id: number;
    name: string;
    imageUrl: string;
}
